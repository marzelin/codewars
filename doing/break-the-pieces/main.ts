// import { pipe } from 'ramda'

// const breakPieces = 
//   pipe(
//     grid,
//     (grid) => pipe(
//       shapeCorners,
//       piecesOriginPoints(grid),
//       piecesCorners(grid),
//       pieces
//     )(grid)
//   )