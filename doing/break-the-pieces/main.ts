// import { pipe } from 'ramda'

// const breakPieces = 
//   pipe(
//     grid,
//     (grid) => pipe(
//       shapeCorners,
//       origins(grid),
//       piecesCorners(grid),
//       pieces
//     )(grid)
//   )